https://www.codewars.com/kata/65e2df8302b29a005831eace

## Python
```py
def hit(l):
    return l%5 if l%5>0 else 1
```

## JavaScript
```js
function hit(l) {
  return l%5 ? l%5 : 1
}
```

## Java
```java
public class Competition {
  static int hit(int l) {
    return l%5>0 ? l%5 : 1;
  }
}
```

## C
```c
int hit(int l) {
  return l%5 ? l%5 : 1;
}
```

## C#
```c#
public static class Competition {
  public static int Hit(int l) {
    return l%5>0 ? l%5 : 1;
  }
}
```