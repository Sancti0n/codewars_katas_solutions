https://www.codewars.com/kata/5a3e44f8b6cfd785c300008c

## JavaScript
```js
function output() {
  return decodeURI.name.charAt(isFinite.call.name.length).concat(
    decodeURI.name.charAt(isFinite.length),
    (typeof decodeURI).charAt(isFinite.length.valueOf.length),
    isFinite.name.charAt(isFinite.length.valueOf.length),
    isFinite.name.charAt(isFinite.call.name.length),
    decodeURI.name.charAt(isFinite.length),
    decodeURI.name.charAt(isFinite.call.name.length)
  )
}
```